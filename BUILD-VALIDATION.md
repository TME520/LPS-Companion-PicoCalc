# Build validation — v1.1, 16 September 2026

Completed:

- ARM Cortex-M0+ cross-compilation and linking with Arm GNU Toolchain 14.2.Rel1 and Pico SDK 2.2.0, using the existing pinned local dependencies.
- UF2 verified: 523 blocks, 267,776 bytes, RP2040 family and valid flash addresses.
- Host tests passed: activities across three pages, new activity save/restore, page wrapping, removed menu shortcut, weights, notes, XP, closing days, save failure rollback, history retention and corruption protection.
- Compatibility checked using bytes generated by the original v1.0 serializer and loaded by the v1.1 decoder, preserving activity flags, weights, XP and history.
- v1.0 physical boot and persistence were confirmed by the user.

Pending:

- v1.1 physical device check, including accented labels and the two new activities.
- UF2 launcher compatibility remains untested; this is a standalone BOOTSEL build.

The prebuilt UF2 matches the included v1.1 source. Dependencies are downloaded separately by build.sh; their pinned revisions are unchanged. First-time download paths require GitHub access.
